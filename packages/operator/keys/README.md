# Operator integrity keys (fixture)

These keys attest **securist-operator package integrity** for local deterministic
runtime provenance until a real signed TARX runtime pack exists.

They are **not** TARX model-pack keys and must never be presented as model
synthesis verification.

Private key is monorepo fixture only — rotate when TARX signing lands.
